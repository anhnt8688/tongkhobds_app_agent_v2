include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # freezed places @JsonKey/@Default on constructor params, which the
    # analyzer flags as invalid_annotation_target. Safe to ignore.
    invalid_annotation_target: ignore
  exclude:
    - "**/*.freezed.dart"
    - "**/*.g.dart"

linter:
  rules:
